# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ui\ui-checkout.spec.js >> Checkout flow for standard_user
- Location: tests\ui\ui-checkout.spec.js:10:3

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator:  locator('.complete-header')
Expected: "THANK YOU FOR YOUR ORDER"
Received: "Thank you for your order!"
Timeout:  5000ms

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('.complete-header')
    13 × locator resolved to <h2 class="complete-header" data-test="complete-header">Thank you for your order!</h2>
       - unexpected value "Thank you for your order!"

```

```yaml
- heading "Thank you for your order!" [level=2]
```

# Test source

```ts
  1  | import { expect } from '@playwright/test';
  2  | 
  3  | export class CheckoutPage {
  4  |   constructor(page) {
  5  |     this.page = page;
  6  |     this.firstName = page.locator('[data-test="firstName"]');
  7  |     this.lastName = page.locator('[data-test="lastName"]');
  8  |     this.postalCode = page.locator('[data-test="postalCode"]');
  9  |     this.continueButton = page.locator('[data-test="continue"]');
  10 |     this.finishButton = page.locator('[data-test="finish"]');
  11 |     this.completeHeader = page.locator('.complete-header');
  12 |   }
  13 | 
  14 |   async fillCheckoutInformation(firstName, lastName, postalCode) {
  15 |     await this.firstName.fill(firstName);
  16 |     await this.lastName.fill(lastName);
  17 |     await this.postalCode.fill(postalCode);
  18 |     await this.continueButton.click();
  19 |   }
  20 | 
  21 |   async finishCheckout() {
  22 |     await this.finishButton.click();
  23 |   }
  24 | 
  25 |   async expectComplete() {
> 26 |     await expect(this.completeHeader).toHaveText('THANK YOU FOR YOUR ORDER');
     |                                       ^ Error: expect(locator).toHaveText(expected) failed
  27 |   }
  28 | }
  29 | 
```